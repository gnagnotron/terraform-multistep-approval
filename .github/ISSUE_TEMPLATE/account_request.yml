name: Subaccount Request
description: Request the provisioning of BTP subaccounts for a project
title: "[ACCOUNT REQUEST]"
labels: [account-request]
body:
  - type: markdown
    attributes:
      value: |
        Use this form to provision BTP subaccounts for a project.
        Do not include passwords or tokens here.

  - type: input
    id: globalaccount
    attributes:
      label: Global Account Subdomain
      description: Value for TF_VAR_globalaccount (example 123abc456trial-ga)
      placeholder: 123abc456trial-ga
    validations:
      required: false

  - type: input
    id: project-name
    attributes:
      label: Project Name
      description: Value for TF_VAR_project_name
      placeholder: Project ABC
    validations:
      required: true

  - type: input
    id: cost-center
    attributes:
      label: Cost Center
      description: Value for TF_VAR_project_costcenter (5 digits)
      placeholder: "12345"
    validations:
      required: true

  - type: textarea
    id: subaccount-full-admins
    attributes:
      label: Subaccount Full Admin Users
      description: One email per line (mapped to TF_VAR_subaccount_full_admins)
      placeholder: |
        user1@example.com
        user2@example.com
    validations:
      required: true
